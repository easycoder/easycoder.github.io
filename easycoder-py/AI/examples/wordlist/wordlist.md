# Word list

## AI prompt
Call a specified URL and read the data it returns. Examine the text and separate out all distinct words; that is, groups of alphabetic characters bounded by any non-alphabetic characters. Then sort the resulting list into alphabetical order, list unique words and count the number of occurrences of each one. Print the list of words and word counts.
